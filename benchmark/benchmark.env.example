# Remote benchmark configuration
# Copy to benchmark.env and fill in values.

EC2_HOST=10.0.0.1
EC2_USER=ec2-user
EC2_KEYPATH=~/.ssh/my-key.pem

# Remote working directory (binaries + traces deployed here)
EC2_REMOTE_DIR=/tmp/valkey-bench
